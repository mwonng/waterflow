#!/usr/bin/env node
var func = require('./func/func');

const main = (i, j, k) => {
  let glassCapacity = 250;    // preset glass capacity

  // conver input to int
  i = parseInt(i, 10);
  j = parseInt(j, 10);
  k = parseInt(k, 10);
}
